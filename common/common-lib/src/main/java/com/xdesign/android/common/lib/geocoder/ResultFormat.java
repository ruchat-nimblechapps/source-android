package com.xdesign.android.common.lib.geocoder;

public enum ResultFormat {

    /**
     * For returning just the first line of a result's address.
     */
    FIRST_LINE,
    /**
     * For returning all lines, merged with a delimiter.
     */
    ALL_LINES
}
